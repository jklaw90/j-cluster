- hosts: all
  become: yes
  tasks:
    - name: create the 'julian' user
      user: name=julian append=yes state=present createhome=yes shell=/bin/bash

    - name: allow 'julian' to have passwordless sudo
      lineinfile:
        dest: /etc/sudoers
        line: 'ubuntu ALL=(ALL) NOPASSWD: ALL'
        validate: 'visudo -cf %s'

    - name: set up authorized keys for the julian user
      authorized_key: user=ubuntu key="{{item}}"
      with_file:
        - ~/.ssh/id_rsa.pub
